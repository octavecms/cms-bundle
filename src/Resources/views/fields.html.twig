{% block media_image_widget %}
    <div id="{{ id }}_widget" class="form-control-image">

        {{ block('hidden_widget') }}

        <div class="form-control-image__preview well well-small">
            {% set value = form.vars.value %}
            <img src="{{ value }}" alt="" class="{% if not value %}hidden{% endif %}" />
        </div>

        <div class="form-control-image__content">

            {% if form.parent.translations is defined %}
                {{ form_row(form.parent.translations) }}
            {% endif %}

            <div class="btn-group">
                <a class="btn btn-primary sonata-action-element js-image-change">
                    {{ 'change_image' | trans({}, 'VideInfraCMSBundle') }}
                </a>
                <a class="btn btn-danger sonata-action-element js-image-reset">
                    <i class="fa fa-trash-o" aria-hidden="true"></i>
                </a>
            </div>
        </div>

    </div>
    <script>
        $(function () {
            $('#{{ id }}_widget').image();
        });
    </script>
{% endblock %}


{% block media_gallery_widget %}
    <div id="{{ id }}_widget" class="form-control-gallery">

        {{ block('hidden_widget') }}

        {% set html %}
            <li>
                {# Item order, we use "galleryorder" to make sure we don't conflict with blocks #}
                <input type="hidden" name="...[galleryorder]" class="form-control" value="0" />

                {# Prototype is media_image_widget #}
                {# {{ form_widget(prototype) }} #}

                @TODO HTML for gallery items same as for media_image_widget, __name__ in will be replaced with
                a number
            </li>
        {% endset %}

        <ul class="form-control-gallery__list js-gallery-list" data-prototype="{{ html|e('html_attr') }}">
            {# @TODO List of images, each image is of type media_image_widget wrapped in <li>...</li> #}
        </ul>

        <div class="btn-group">
            <a class="btn btn-primary sonata-action-element js-gallery-add">
                <i class="fa fa-plus"></i>
                {{ 'add_image' | trans({}, 'VideInfraCMSBundle') }}
            </a>
        </div>

    </div>
    <script>
        $(function () {
            $('#{{ id }}_widget').gallery();
        });
    </script>
{% endblock %}