{% extends '@SonataAdmin/CRUD/base_list_field.html.twig' %}
{% block field %}
    {% if object.isActive %}
        <a target="_blank" href="{{ path(object.name) }}">{{ object.path }}</a>
    {% else %}
        {{ object.path }}
    {% endif %}
{% endblock %}