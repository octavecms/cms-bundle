{% macro form_widget_search (attr) -%}
    {%- from 'partials/blocks/form_widget_simple.twig' import form_widget_simple -%}

    {%- set widget_attr = attr.attr|default({}) -%}
    {%- set widget_attr = {}|merge(widget_attr)|merge({'prefixIcon': 'search'}) -%}
    {%- set attr = {}|merge(attr)|merge({'type': 'search', 'attr': widget_attr}) -%}

    {{- form_widget_simple(attr) -}}

    {# @TODO
    {% include 'partials/form/text.twig' with {
                name: 'search',
                className: 'form-control--small form-control--pill input-group--small input-group--pill',
                placeholder: 'Search',
                prefixIcon: {
                    id: 'search',
                    width: 15,
                    height: 16
                }
            } %} #}
{%- endmacro %}