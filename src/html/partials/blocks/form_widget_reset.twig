{% macro form_widget_reset (attr) -%}
    @TODO
{%- endmacro %}

{% macro reset_widget (attr) -%}
    {%- from 'partials/blocks/form_widget_button.twig' import button_widget -%}

    {%- set attr = {}|merge(attr|default({})) -%}
    {%- set attr = attr|merge({'type': 'reset'}) -%}

    {{- button_widget(attr) -}}
{%- endmacro %}